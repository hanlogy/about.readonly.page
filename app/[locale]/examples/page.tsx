import { MainContainer } from '@/components/MainContainer';

export default async function ExamplesHome({}: PageProps<'/[locale]/examples'>) {
  return (
    <MainContainer>
      <div className="py-20">Under construction...</div>
    </MainContainer>
  );
}
